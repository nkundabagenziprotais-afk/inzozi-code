# Example only.
#
# Install as:
# /srv/inzozi-code/secrets/gotify.env
#
# Required: root ownership, mode 0600.
#
# Resolve and verify the immutable digest before
# staging installation. Never use :latest.

GOTIFY_IMAGE=gotify/server:3.1.0@sha256:REPLACE_WITH_EXACT_64_HEX_DIGEST
