# Example only. Copy to /srv/inzozi-code/secrets/restic.env as root,
# set mode 0600, and replace placeholders out-of-band.
# Never commit the populated file.

RESTIC_REPOSITORY='replace-with-off-host-restic-repository'
RESTIC_PASSWORD_FILE='/srv/inzozi-code/secrets/restic-password'

# Add only backend-specific credentials required by the selected Restic backend.
# Examples include AWS_ACCESS_KEY_ID/AWS_SECRET_ACCESS_KEY for S3-compatible
# storage, but real values belong only in the root-readable host secret file.
